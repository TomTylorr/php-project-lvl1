name: lint

on:
    push:
        branches:
            - '**'
        tags:
            - '**'

jobs:
  run-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Run the linter
        run: make lint

